import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.develop.app',
  appName: 'Develop',
  webDir: 'dist/develop',
  bundledWebRuntime: false
};

export default config;
